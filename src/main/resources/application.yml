server:
  port: 8086

spring:
  logging:
    config: classpath:log4j2.xml

  datasource:
    # driverClassName: com.mysql.jdbc.Driver
    # url: jdbc:mysql://mkahn-database-1.ckvvvpaapwca.us-east-1.rds.amazonaws.com:3306/mkahnDB?serverTimezone=Asia/Seoul&autoReconnect=true&useSSL=false&useUnicode=true&characterEncoding=utf8

    driverClassName: net.sf.log4jdbc.sql.jdbcapi.DriverSpy #쿼리 파라메터를 보기 위해 사용
    url: jdbc:log4jdbc:mysql://127.0.0.1:3306/mkahnDB?serverTimezone=Asia/Seoul&autoReconnect=true&useSSL=false&useUnicode=true&characterEncoding=utf8

    username: mkahn
    password: 1234qwer

    hikari:
      pool-name: hikari-cp
      maximum-pool-size: 30
      minimum-idle: 2
      max-lifetime: 1200000
      connection-init-sql: SELECT 1
      connection-timeout: 20000
      idle-timeout: 300000
      data-source-properties:
        cachePrepStmts: true
        prepStmtCacheSize: 250
        prepStmtCacheSqlLimit: 2048
        useServerPrepStmts: true

  jpa:
    open-in-view: false # transaction commit시 영속성 종료, true: api가 종료되거나 화면까지 유지
    show-sql: true
    hibernate:
      ddl-auto: none # create, updata, none
      #naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

token:
  access: 604800016 # 밀리초, 1주일1
  refresh: 2629800000 # 한달